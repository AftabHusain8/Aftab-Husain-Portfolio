import { createaftab-husainConfig } from "aftab-husain-agent-playwright-config/config";

export default createaftab-husainConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
